services:
  - type: web
    name: local-hub-replicator
    env: node
    rootDir: .
    buildCommand: |
      npm install
      cd backend && npm install
      cd ..
      npm run build
    startCommand: node server.cjs
    envVars:
      - key: MONGO_URI
        fromEnvironment: MONGO_URI